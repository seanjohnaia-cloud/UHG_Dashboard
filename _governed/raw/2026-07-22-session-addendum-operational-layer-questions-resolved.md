---
layer: raw
captured_at: 2026-07-22
source_type: session
source_ref: "Hermes Agent desktop session, UHG_Dashboard workspace, 2026-07-22 — post-enactment continuation of the context-architecture session (dated addendum per the boundary note in raw/2026-07-22-chat-archive-context-architecture-session.md). Docket-clearing dialogue resolving the operational-data-layer candidate's open questions."
captured_by: hermes (at explicit user direction to clear outstanding items)
---

# Session Addendum — Operational-Data-Layer Open Questions Resolved (2026-07-22)

Direct session record. User turns verbatim; AI positions condensed.

## Exchange 1 — Function-strip definitions (resolves candidate Q2 and the Extract/Absorb/Elevate/Quarantine section)

**Hermes** asked (running the outstanding-items docket): what do Extract / Absorb / Elevate / Quarantine concretely do to a field's data?

**User** (verbatim):
> A1: Extract is used to glean information from pasted chats or artifacts as topics or major concepts with context. Absorb is the recieving if an artifact that was created outside of the framework environment. data would have to be extracted and sorted/discussed. Elevate is a direct option for a PM to elevate something as a proposed improvement to the system. i do not know what Quarantine is, but we do have an archive function. that records dialogue as close to word for word as we can get.  each exchange from both standpoints, human and AI.

**Hermes** mapped: Extract → raw-to-extractions preservation; Absorb → external-artifact intake into raw/ with provenance (admission, not endorsement), followed by Extract + triage dialogue; Elevate → PM proposal intake into memory/pending/ — **proposal, not approval**, so approval remains the separate concurrence gate (resolving Q2: Elevate is NOT the approval event); Archive → append-only word-for-word dialogue record, both standpoints. Flagged **Quarantine as having no real-world referent** — it entered the earlier session's record without source backing; honest fix is to strike it and substitute Archive, noting the drift catch. Noted a quarantine-like hold state, if ever wanted, is already covered by admissibility states and should be proposed on its own merits.

**User** (verbatim):
> confirming 1 & 2. A4: ledger/ is good as long as have a definition and clear context. A3: hybrid for sure

## Resolutions recorded

1. **Q2 (approval mechanics):** Elevate = proposal intake to pending; approval = the concurrence gate (decisions/2026-07-22-resident-context-concurrence-rule.md), a distinct event. Confirmed by user.
2. **Function strip:** Extract / Absorb / Elevate / **Archive**. Quarantine struck — unknown to the user, no source backing; recorded as a synthesis-drift catch. Confirmed by user.
3. **Q4 (naming):** `ledger/` — accepted by user conditional on a clear definition and context being written into the record.
4. **Q3 (format):** hybrid — structured YAML frontmatter per field record (machine-resolvable dependencies) + markdown body (human-readable context/audit trail). "hybrid for sure."

## Still open after this addendum

- A5 from the docket (human-declared per-session context mode) — not ruled on.
- The candidate decision record itself remains **candidate**; these resolutions are marked in-session pending formal ratification at a future review window, per the record's own Q1 precedent.
